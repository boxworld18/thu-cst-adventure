#include <cmath>

//functions on this page-------------//
double func31(double);
double func32(double);
//-----------------------------------//

double func31(double x) {
    return sin(x) * 2 - sin(x * 2);
}
double func32(double x) {
    return cos(x) * 2 - cos(x * 2);
}
